"""Purpose: build CanonicalRecord objects with deterministic IDs."""

# TODO
# - Implement deterministic canonical ID derivation.
# - Assemble normalized messages and flags into canonical records.

def build_canonical(raw_records: list[object]) -> list[object]:
    """Stub: build canonical records from raw records."""
    raise NotImplementedError
